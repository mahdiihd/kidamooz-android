# مشخصات پروژه اپ و وب کیدآموز

این سند، ساختار فعلی مشاهده‌شده در کد را ثبت می‌کند. نام فایل مطابق درخواست `spec.md` است.

## نقش و فناوری

اپ قصه کودک و نسخه وب/PWA از یک کدبیس Angular 20.3، Ionic 8 و Capacitor 8 ساخته می‌شوند. زبان توسعه TypeScript و سبک‌ها SCSS هستند. شناسه اپ در `capacitor.config.ts` برابر `com.kidamooz.app` و پوشه خروجی وب `www` است.

## قابلیت‌های مشاهده‌شده

مسیرهای برنامه شامل خانه، فهرست قصه، پخش قصه، ورود عضو، قصه‌های من و مراحل ساخت قصه هستند. مسیر والدین تابع تنظیم قابلیت و در حالت فعال به صفحه در دست ساخت متصل است. سرویس‌های پخش و کش صوت، علاقه‌مندی‌ها، پروفایل کودک، اعلان، ضبط صدا و به‌روزرسانی PWA در کد وجود دارند؛ وجود سرویس به‌تنهایی تأیید صحت عملیاتی آن نیست.

## نقشه کد

- `src/app/app.routes.ts`: مسیرها و محافظ‌های دسترسی.
- `src/app/features`: صفحات و پوسته زبانه‌ها.
- `src/app/core/services`: کلاینت API، وضعیت کاتالوگ، پخش، ذخیره‌سازی و قابلیت‌های دستگاه.
- `src/app/core/models` و `src/app/core/mappers`: مدل‌ها و تبدیل داده‌ها.
- `src/app/shared`: اجزای مشترک.
- `src/environments`: محیط توسعه و `environment.prod.ts` برای production.
- `src/global.scss` و `src/theme/variables.scss`: سبک‌ها و تم.
- `ngsw-config.json` و `public/manifest.webmanifest`: تنظیمات PWA.
- `android/`: پروژه بومی Android؛ با پوشه ریشه این اپ اشتباه نشود.

## دستورات

از ریشه همین پروژه اجرا کنید:

```powershell
npm ci
npm start
npm run lint
npm run build:pwa
npm test -- --watch=false --browsers=ChromeHeadless
```

آزمون‌ها Karma/Jasmine هستند و اجرای Headless به Chrome نیاز دارد. نصب وابستگی‌ها اسکریپت `patch-package` را نیز اجرا می‌کند. پس از ساخت و برای کار بومی، `npm run cap:sync` و سپس `npm run cap:android` در دسترس‌اند. اجرای بومی به ابزارهای Android نیاز دارد.

## قرارداد و بررسی

قرارداد درخواست‌ها را از سرویس‌های API و کنترلرهای پروژه `../Back` دنبال کنید. هنگام تغییر پخش، ضبط یا اعلان، هر دو مسیر وب و بومی و چرخه عمر برنامه را بررسی کنید. محافظ ورود سمت کاربر جایگزین مجوز سرور نیست. جزئیات انتشار در [WEB.md](WEB.md) است. این بررسی مستنداتی شامل اجرای ساخت یا آزمون دستگاه نبوده است.
